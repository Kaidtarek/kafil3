import 'package:flutter/material.dart';


class welcom_emp extends StatelessWidget {
  const welcom_emp({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.amber,
      body: Center(child: Text('hii')),
    );
  }
}