import 'package:flutter/material.dart';
class houses extends StatelessWidget {
  const houses({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('house'),),
    );
  }
}